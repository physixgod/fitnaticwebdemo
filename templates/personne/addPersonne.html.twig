<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fitness Sign Up</title>
    <!-- Add links to your CSS stylesheets or include Bootstrap styles -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <!-- Add any additional styling for your fitness interface -->
    <style>
        body {
            background-color: #001f3f; /* Dark blue background */
            color: #ffffff; /* White text color */
        }

        .signup-container {
            max-width: 400px;
            margin: auto;
            margin-top: 50px;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            background-color: #00263e; /* Darker blue background */
        }

        .form-group {
            margin-bottom: 20px;
        }

        .signup-title {
            margin-bottom: 30px;
        }

        .countdown {
            font-size: 20px;
            color: red;
        }

        /* Updated to orange button */
        .btn-primary {
            background-color: #ff8c00 !important; /* Orange color */
            border-color: #ff8c00 !important;
        }
    </style>
</head>

<body>
    <div class="container signup-container">
        <div align="center">
            <h2 class="signup-title">Sign up</h2>

            {{ form_start(form) }}

            <div class="form-group">
                {{ form_row(form.nom, { 
                    'attr': {'class': 'form-control', 'placeholder': 'Nom'}  }) }}
            </div>

            <div class="form-group">
                {{ form_row(form.prenom, { 'attr': {'class': 'form-control', 'placeholder': 'Prénom'} }) }}
            </div>
            <div class="form-group">
                {{ form_row(form.email, { 'attr': {'class': 'form-control', 'placeholder': 'Email'} }) }}
            </div>
            <div class="form-group">
                {{ form_row(form.genre, { 'attr': {'class': 'form-control', 'placeholder': 'Genre'} }) }}
            </div>
            <div class="form-group">
                {{ form_row(form.dateNaissance, { 'attr': {'class': 'form-control'} }) }}
            </div>
            <div class="form-group">
                {{ form_row(form.motDePasse, { 'attr': {'class': 'form-control', 'placeholder': 'Mot de Passe'} }) }}
            </div>
            <div class="form-group">
                {{ form_row(form.motDePasse2, { 'attr': {'class': 'form-control', 'placeholder': 'Confirmer le Mot de Passe'} }) }}
            </div>

            <button type="submit" class="btn btn-primary btn-block">Sign up</button>

            {{ form_end(form) }}
            {% for flashError in app.flashes('error') %}
                <div class="alert alert-danger" role="alert" style="margin-top: 30px; width: 600px; margin-left: auto; margin-right: auto;">
                    {{ flashError }}
                </div>
            {% endfor %}
        </div>
    </div>

    <!-- Add links to your JS scripts or include Bootstrap scripts if needed -->
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>

</html>

