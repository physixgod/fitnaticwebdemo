<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ competition.name }} Details</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            color: #343a40;
            margin: 0;
            padding: 0;
        }

        .section {
            background-color: #fff;
            padding: 40px;
            margin-top: 30px;
            border-radius: 8px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }

        .section-heading {
            margin-bottom: 30px;
        }

        h2 {
            font-size: 3rem;
            color: #007bff;
            margin-bottom: 10px;
        }

        p {
            font-size: 1.5rem;
            line-height: 1.8;
            color: #6c757d;
        }

        ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        li {
            margin-bottom: 30px;
        }

        h4 {
            font-size: 2rem;
            color: #343a40;
            margin-bottom: 15px;
        }

        p {
            font-size: 1.5rem;
            color: #6c757d;
        }

        .announcement {
            background-color: white;
            color: #fff;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 30px;
        }

        .btn {
            display: inline-block;
            padding: 15px 30px;
            font-size: 1.5rem;
            color: #fff;
            background-color: #e85d39;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .btn:hover {
            background-color: #d14827;
        }

        table {
            width: 100%;
           
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #dee2e6;
            padding: 15px;
            text-align: left;
        }

        th {
            background-color: #007bff;
            color: #fff;
        }
    </style>
</head>
<body>
    <section class="section" id="competition-details">
        <div class="container" align="center">
            <div class="row">
                <div class="col-lg-12 text-center announcement">
                    <h2>Exciting Competition Announcement!</h2>
                    <p>Get ready for an amazing competition experience. Here are the details:</p>
                </div>
                <div class="col-lg-6 offset-lg-3">
                    <div class="section-heading text-center">
                        <h2>{{ competition.name }} </h2>
                        <p>{{ competition.description }}</p>
                    </div>
                </div>
                <div class="col-lg-12 text-center mt-4">
                    <table>
                        <tr>
                            <th>Start Date</th>
                            <th>End Date</th>
                        </tr>
                        <tr>
                            <td>{{ competition.startDate|date('Y-m-d') }}</td>
                            <td>{{ competition.endDate|date('Y-m-d') }}</td>
                        </tr>
                        <tr>
                            <th>Location</th>
                            <th>Sport Type</th>
                        </tr>
                        <tr>
                            <td>{{ competition.location }}</td>
                            <td>{{ competition.sportType }}</td>
                        </tr>
                        <tr>
                            <th>Entry Fee</th>
                            <th>Participants</th>
                        </tr>
                        <tr>
                            <td>{{ competition.entryFee }}</td>
                            <td>{{ competition.maxParticipants }}</td>
                        </tr>
                    </table>
                </div>
            </div>
        </div>
    </section>
</body>
</html>
