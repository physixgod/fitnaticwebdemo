{# templates/publication/new.html.twig #}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create new Publication</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('publication_index') }}">Back to the list</a>
{% endblock %}
